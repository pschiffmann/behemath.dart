/// A variable or function name.
const String identifier = 'identifier';

/// Tokens that contain a number, in integer or double notation.
const String number = 'number';

/// For prefix operators, like the prefix `-` in `-4`.
const String prefixOperator = 'prefixOperator';

/// For infix operators, like `+` in `4 + 3`.
const String infixOperator = 'infixOperator';

/// The term _fence operator_ is borrowed from the Mozilla MathML documentation.
/// This token type is used for brackets and "absolute value" bars.
// const String fenceOperator = 'fenceOperator';

const String lparen = '(';
const String rparen = ')';
const String lbracket = '[';
const String rbracket = ']';
const String lbrace = '{';
const String rbrace = '}';
const String bar = '|';
